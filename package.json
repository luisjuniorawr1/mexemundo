{
  "name": "mexemundo",
  "version": "0.7.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "check": "node --check server.js && node --check public/js/realtime.js && node --check public/js/tv.js && node --check public/js/phone.js && node --check public/js/goalkeeper.js"
  },
  "engines": {
    "node": ">=18"
  },
  "description": "Plataforma de minijogos infantis controlados por movimentos usando o celular como sensor corporal.",
  "dependencies": {
    "ws": "^8.21.1"
  }
}
