{
  "name": "mexemundo",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "check": "node --check server.js && node --check public/js/realtime.js && node --check public/js/tv.js && node --check public/js/phone.js"
  },
  "engines": {
    "node": ">=18"
  },
  "description": "Protótipo de minijogos infantis controlados por movimentos usando o celular como sensor corporal."
}
